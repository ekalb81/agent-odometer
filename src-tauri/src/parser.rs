/// Parses a Codex JSONL session file into a Session struct.
/// Phase 2 will implement the full parsing logic against sample-session.jsonl.
pub fn parse_session_file(_path: &std::path::Path) -> anyhow::Result<crate::model::Session> {
    unimplemented!("Phase 2")
}
